---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { localeFromPath, t } from '@/lib/i18n';

const locale = localeFromPath(Astro.url.pathname);
Astro.response.status = 404;
---

<BaseLayout
    locale={locale}
    title={t(locale, 'error.notFound.title')}
    description={t(locale, 'error.notFound.description')}
>
    <section class="detail-page">
        <header class="detail-header">
            <p class="eyebrow">404</p>
            <h1>{t(locale, 'error.notFound.title')}</h1>
            <p class="lede">{t(locale, 'error.notFound.description')}</p>
        </header>
    </section>
</BaseLayout>
