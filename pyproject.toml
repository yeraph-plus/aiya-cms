[build-system]
requires = ["setuptools==83.0.0", "wheel==0.47.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aiya-cms"
version = "0.1.0"
description = "A modular headless CMS foundation with an integrated admin SPA"
requires-python = ">=3.14,<3.15"
dependencies = [
  "aiosmtplib==5.1.2",
  "alembic==1.18.5",
  "apscheduler==3.11.3",
  "asyncpg==0.31.0",
  "boto3==1.43.67",
  "cryptography==50.0.0",
  "fastapi==0.141.1",
  "markdown-it-py==4.2.0",
  "pydantic==2.13.4",
  "pydantic-settings==2.14.2",
  "PyJWT==2.13.0",
  "pwdlib[argon2]==0.3.0",
  "paypal-server-sdk==2.3.0",
  "Pillow==12.1.1",
  "python-multipart==0.0.22",
  "python-slugify==8.0.4",
  "redis==8.1.0",
  "requests==2.34.2",
  "sqlalchemy[asyncio]==2.0.51",
  "structlog==26.1.0",
  "uvicorn[standard]==0.41.0",
  "uuid-utils==0.17.0",
]

[project.optional-dependencies]
dev = [
  "aiosqlite==0.21.0",
  "bottle==0.13.4",
  "brotli==1.2.0",
  "httpx==0.28.1",
  "mypy==2.3.0",
  "pytest==9.1.1",
  "pytest-asyncio==1.4.0",
  "ruff==0.16.1",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["inc*"]

[tool.pytest.ini_options]
addopts = "-ra"
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py314"
line-length = 100
src = [".", "tests"]
exclude = ["context"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]

[tool.ruff.lint.per-file-ignores]
# FastAPI routers declare Depends/Path/query dependencies in argument defaults
"inc/api/http/*.py" = ["B008"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.14"
strict = true
files = ["inc"]
plugins = []
