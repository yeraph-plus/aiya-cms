"""OpenList content delivery adapter (planned).

Target: unspecified content-side Port (content capability has no Port yet;
decide before implementation).

Planned integration: OpenList (openlist.app) content distribution/list
SDK. Target Port and contract must be frozen before implementation; this
file must stay import-safe and side-effect free.
"""
