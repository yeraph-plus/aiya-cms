# Kernel / <组件名>

> 模板使用说明：所有章节必须保留；无内容的章节写"无"并附一句原因。本文档是代码的前置规格，实现与本文档不一致时先改文档（SDD）。

## 1. 设计目的

<这个组件为什么存在？解决什么问题？不解决什么问题（非目标）？>

## 2. 范围与依赖

- 代码位置: `inc/kernel/<component>/`
- 依赖的 kernel 组件: <列表，或"无">
- 被谁依赖: <列表>
- 外部依赖: <三方库/基础设施>

## 3. 领域模型

<核心概念、实体、值对象及其关系。文字或简图。>

## 4. 状态机

<状态枚举 + 合法转换表；无状态写"无"。>

| 当前状态 | 事件/动作 | 下一状态 | 备注 |
|---|---|---|---|

## 5. 数据库

### 表: `<table_name>`

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|

索引: <列表>
JSONB 字段对应的 Pydantic Model: `<ModelClass>`（定义位置）

## 6. 公开 API（kernel 内部接口）

<暴露给 modules / api 的 Python 接口签名（Protocol/类/函数）。这是封装边界，稳定承诺。>

### HTTP API（如有）

| 方法 | 路径 | Capability | 请求 DTO | 响应 DTO | 说明 |
|---|---|---|---|---|---|

## 7. Pipeline

- 拥有的 Pipeline key: <列表，或"无">
- 注入点: before / after
- 已登记 step: <列表>

## 8. Event

- 发布: <事件类型清单 + payload 模型>
- 订阅: <监听的事件 + 处理器 + 用途>

## 9. 错误码

| 错误码 | HTTP | 含义 | 触发条件 |
|---|---|---|---|

## 10. Cron / 任务

<定时任务、BaseTask 子类、LISTEN/NOTIFY 使用情况；无则写"无"。>

## 11. 测试边界

<必须锁定的行为清单：接口契约、流程、边界值、异常路径。每条对应至少一个 pytest。>

## 12. 未决事项

<遗留问题、后期扩展预留。>
